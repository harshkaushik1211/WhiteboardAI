# XTTS Hindi Finetuned — Benchmark Dependencies
# Install into the backend venv: pip install -r xtts_benchmark/requirements.txt

# Coqui TTS (Idiap community fork — supports Python 3.13, includes XTTS-v2)
coqui-tts>=0.27.0

# Audio processing
soundfile>=0.12.0
librosa>=0.10.0

# Performance monitoring
psutil>=5.9.0
